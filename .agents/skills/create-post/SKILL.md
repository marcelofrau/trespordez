# Create Post

Read `AGENTS.md` and `docs/content-model.md`. Create draft Markdown in `_drafts/` unless user explicitly requests publication. Preserve author voice, use known category/tag keys, add description and local media metadata, then run validation. For reviews, use existing score-card structure and approved ReffPixels icons only from `assets/images/ui/review/`. Do not publish raw `_library` assets or push.
