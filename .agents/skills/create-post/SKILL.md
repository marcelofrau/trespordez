# Create Post

Read `AGENTS.md` and `docs/content-model.md`. Create draft Markdown in `_drafts/` unless user explicitly requests publication. Preserve author voice, use known category/tag keys, add description and local media metadata, then run validation. Do not push.
