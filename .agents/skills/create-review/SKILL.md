# Create Review

Read `AGENTS.md`, `docs/content-model.md`, and `docs/handoff.md`. Draft in `_drafts/` unless publication is explicitly requested. Preserve writer voice and opinion. Add supported tags, including `indie` only with clear evidence. Use front matter `image_position` or `image_fit: contain` to preserve title art in cards. Use `.review-score` structure with color and approved ReffPixels icons from `assets/images/ui/review/`. Put screenshot sequences in `.post-gallery` for PhotoSwipe. Validate. Do not push.
