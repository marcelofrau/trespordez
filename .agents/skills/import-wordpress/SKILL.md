# Import WordPress

Use `scripts/import-wordpress.mjs` with saved API JSON. Preserve permalink, dates, taxonomy, external links and media. Remove WordPress runtime markup. Inspect generated content before any commit.
