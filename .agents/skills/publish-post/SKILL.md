# Publish Post

Only after explicit approval: validate, move draft into `_posts/`, generate missing tag pages if tags changed, commit with personal identity, push `main`, wait for GitHub Pages QA workflow to pass, then report QA URL. Never use corporate GitHub identity.
