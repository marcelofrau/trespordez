# Review QA

Read `docs/handoff.md`. Check QA desktop and mobile for route, local asset, navigation, card crop, hero, table, gallery, iframe, Giscus, RSS and sitemap behavior. Run `node scripts/validate-site.mjs`; run `bundle exec jekyll build` when Ruby is available. Report exact page URLs and defects. Do not publish without approval.
