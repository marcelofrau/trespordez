# Update Platform

Read `AGENTS.md` and `docs/handoff.md`. Existing editorial platform pages are in `_pages/wordpress/`; initial catalogs are `_pages/platforms/`. Preserve route and `emulator: true`. Link only official project sources. Do not claim recommendation/testing unless source content supports it. Keep system card in `_data/platforms.yml` synchronized. Use hero image, zebra table styling, and PhotoSwipe galleries. Validate. Do not push.
